---

---

{
    import.meta.env.BUILD_MODE === "production" && (
        <script>// add analytics scripts here for production only</script>
    )
}

{
    import.meta.env.BUILD_MODE === "staging" && (
        <script>// add analytics scripts here for staging only</script>
    )
}

<!--add analytics scripts here for all environments-->
